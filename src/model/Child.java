package model;

public class Child extends Person {
    
    public Child(int x, int y, boolean mask, boolean vulnerable) {
        super(x, y, mask, vulnerable);
        // image = "child.jpeg"
        // profile = "ChildProfile.jpeg"
    }

}
