package model;

public class Adult extends Person {
    
    public Adult(int x, int y, boolean mask, boolean vulnerable) {
        super(x, y, mask, vulnerable);
        // profile = "AdultProfile.jpeg"
    }
    
}
